# Score relay URL (the /live namespace for subscribing to match events)
VITE_RELAY_URL=http://localhost:8384

# Factory server URL (for fetching tournament roster — participants, images, jerseys)
VITE_FACTORY_URL=http://localhost:8383
